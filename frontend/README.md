# Frontend (placeholder)

This project’s current scope is **backend-only model training**.

When you’re ready to build the UI (React + Tailwind + Recharts), this folder is where it should live.

