# GitHub project description (polished)

Pollution Regime Classification is a full-stack machine learning analytics application that detects latent pollution regimes from air-quality time series using Hidden Markov Models (HMM). It supports dataset upload, HMM training, persisted regime predictions, and an interactive React dashboard with transition analytics, confidence trends, and historical run auditing.

